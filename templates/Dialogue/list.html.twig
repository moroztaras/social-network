{% for dialogue in dialogues %}
    {% if dialogue.creator == user %}
        {{ include('Dialogue/ModeView/full.html.twig',{'dialogue':dialogue, 'user':dialogue.receiver}) }}
    {% else %}
        {{ include('Dialogue/ModeView/full.html.twig',{'dialogue':dialogue, 'user':dialogue.creator}) }}
    {% endif %}
{% endfor %}
